# 知寻 ZhiSeek · 数据战役计划
## τ 拟合 + 垄断度热榜首页 + 指标有效性验证（一次采集，三线复用）

> 核心思路：三件事都需要「大量真实知乎回答的搜索数据」，合并成一次数据采集战役，原料共享、顺序依赖、逐级产出。
>
> 依赖关系：
> ```
> 阶段0（采集）→ 阶段1（τ拟合）→ 阶段2（全量跑通）→ 阶段3（热榜首页）
>                                     ↘ 阶段4（验证）→ 阶段5（固化）
> ```

---

## 阶段 0：共享数据采集（一切的原料）

**目标**：一份样本，喂饱三个任务。

- 用 `hot_list` 接口拉热榜，人工筛 **15–20 个议题**，刻意分层：
  - 热榜时事类（τ 预期小）与常青长尾类（τ 预期大）各半
  - 覆盖科技 / 医学 / 法律 / 情感 / 职场
- 每个议题只跑 pipeline 的**阶段 1（搜索 + 枚举抓取）**，拿到每篇回答的 `votes` + `edit_time` + 正文摘要，全部进缓存
- 配额预算：搜索 5000 次/日、枚举 100 次/日。15–20 议题 ×（~8 搜索 + ~10 枚举）≈ 300 次调用，单日可完成；**建议分两天跑留余量**
- 记录每个议题的抓取元数据（样本量、回答年龄跨度分布），供阶段 1 拟合使用

**产出**：`data/cache/` 下 15–20 个议题的阶段 1 缓存。

---

## 阶段 1：τ 拟合（数据分析师的活，不动 pipeline）

**目标**：用真实数据回答「点赞到底多快饱和」。

- 汇总全部议题样本，按回答年龄分桶：0–7 天 / 7–30 天 / 30–90 天 / 90–365 天 / 1–3 年 / 3 年+
- 每桶取**赞同数中位数**（均值会被万赞回答带飞），画出「中位赞数–年龄」散点
- 最小二乘拟合 `M(t) = M∞·(1−e^{−t/τ})`，解出 M∞ 和 τ
- **分档检验**：时事类 vs 常青类分别拟合，两个 τ 差 >3 倍 → config 分两档（议题类型用 LLM 一行判断）；否则全局单档
- **敏感性分析**（答辩弹药）：τ 在拟合值 ±50% 区间扰动，观察遗珠清单 Top 10 的变动率。若变动 <20%，说明指标对 τ 不敏感，「τ 取不准」的质疑即被化解
- 拟合值写回 `config.EXPO_TAU_DAYS`，保存拟合曲线图（PPT 素材）

**产出**：校准后的 τ（+ 分档决策）、敏感性分析结论、拟合曲线图。

---

## 阶段 2：全量跑通 + 垄断度排名

**目标**：让每个议题产出 `monopoly_gap`。

- 对 15–20 个议题跑完阶段 2–6（拆主张走 glm-4-air，预算个位数人民币/议题，总成本可控）
- 汇总所有议题的垄断度，按 `monopoly_gap` 降序排列
- **人工复核 Top 5**：点开看挖掘出的遗珠是否「真的值得」——同时是阶段 4 的预筛

**产出**：垄断度排行榜数据（一个静态 JSON）。

---

## 阶段 3：垄断度热榜首页

**目标**：Demo 开场从「被动输入」变成「主动发现」。

- 前端加入口：「今日最被垄断的问题」榜单——每条显示问题标题 + 垄断度仪表条 + 「Top3 占 X% 曝光仅贡献 Y% 增量」
- 点击条目直接进该议题的缓存结果（秒开，零现场风险）
- 路演开场改写：「知乎今天热榜上，垄断最严重的问题被 3 篇回答拿走 91% 的曝光——我们点进去看看被埋没了什么」

**产出**：首页榜单 + 新开场叙事。

---

## 阶段 4：指标有效性验证（最后做，用前面积累的真实结果）

**目标**：证明 U 值和人的判断一致。

- 从复核印象最好的 2–3 个议题中抽 **40 篇回答**，刻意混合：遗珠候选 / 中腰部 / 头部高赞各约 1/3
- **盲标**：只看原文，不看系统评分，标「值得更多人看到 / 一般 / 不值得」三档；最好找一位朋友加标一轮，可算双人对齐率
- `scipy.stats.spearmanr` 计算 U 排序 vs 人工排序的相关系数
- 若 ρ < 0.5：这是**调权重信号**——优先怀疑 D 支柱判定质量与 F 惩罚力度，用盲标数据定位拖后腿的分量
- 最终话术：「在 N 篇人工盲标样本上，知寻的价值排序与人工判断相关性 ρ=0.7X」

**产出**：验证结论数字 + 散点图（PPT 核心页）。

---

## 阶段 5：固化

- 手册更新：τ 实测值、验证结论、垄断度实测数据、新开场脚本
- 保底议题用新 τ 重跑一遍（结果会变），重录 60 秒录屏
- Q&A 补两条：
  - 「τ 怎么定的」→ 答：真实数据拟合 + 敏感性分析
  - 「怎么证明挖得准」→ 答：人工盲标相关性

---

## 两个关键顺序决策

1. **τ 拟合必须在全量跑通之前**——否则所有议题要用新 τ 重跑阶段 5，浪费配额
2. **验证放在最后**——盲标样本要用最终版公式（τ 和权重定稿后）的输出做对照，没定稿前标了也白标

---

## 风险与配额红线

| 风险 | 预案 |
| --- | --- |
| 搜索配额耗尽 | 分两日执行阶段 0；枚举通道（100/日）优先保保底议题 |
| 拟合数据量不足（高龄桶样本稀疏） | 阶段 0 选题时刻意加入 2–3 个「古老长青问题」（如经典书籍/职业选择类），保证 3 年+ 桶有样本 |
| glm-4-air 中途不可用 | 保底议题结果已缓存，阶段 2 只补缺口议题；最坏情况回退 zhida-fast（额度 100/日，拆主张批量 6 次/议题可控） |
| 盲标 ρ 不理想 | 视为调参信号而非失败：定位分量问题，调整后在剩余议题上复验——「用数据迭代指标」本身也是路演加分叙事 |
